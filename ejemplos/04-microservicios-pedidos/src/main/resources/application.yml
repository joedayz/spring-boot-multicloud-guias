spring:
  application:
    name: pedidos-service

server:
  port: 8082

dfc:
  catalog:
    base-url: http://localhost:8081

management:
  endpoints:
    web:
      exposure:
        include: health,info
